/*Ejercicio 1:*/
//Funcion recursiva que devueve un rango de números entre dos que ingreso el ususario. En caso de
//ingresar los mismos números devuelve ese mismo número.
//pd: no la termino de enter muy bien. 

/*let numbers=[];

function rangeOfNumbers(startNum, endNum) {
    if (endNum-starNum===0) {
      return [endNum];
    } else {
      numbers = rangeOfNumbers(startNum, endNum - 1);
      numbers.push(endNum);
      return numbers;
    }
  }
  rangeOfNumbers(1, 5);*/
